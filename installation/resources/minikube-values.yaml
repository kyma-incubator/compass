global:
  isLocalEnv: true
  externalServicesMock:
    enabled: true

gateway:
  gateway:
    auditlog:
      enabled: true
      authMode: "oauth"

director:
  deployment:
    allowJWTSigningNone: true
