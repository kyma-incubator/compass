apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  name: istio-system
spec:
  values:
    global:
      proxy:
        resources:
          requests:
            cpu: 20m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 128Mi

