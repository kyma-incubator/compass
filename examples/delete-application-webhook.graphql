mutation {
			result: deleteApplicationWebhook(webhookID: "ecdef34c-b89c-410d-8598-84380d3caf77")  {id
		type
		url
		auth {
		  credential {
				... on BasicCredentialData {
					username
					password
				}
				...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				}
			}
			additionalHeaders
			additionalQueryParams
		
		}}
		}