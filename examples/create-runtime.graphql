mutation {result: createRuntime(in: {
		name: "runtime-1",
		description: "runtime-1-description",
		labels: {
			ggg: ["hhh" ]
	}
		,
		annotations: {
			kkk: "lll"
	}
		
	}) {id
		name
		description
		tenant
		labels 
		annotations
		status {condition timestamp}
		agentAuth {credential {
				... on BasicCredentialData {
					username
					password
				}
				...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				}
			}
			additionalHeaders
			additionalQueryParams
			requestAuth { 
			  csrf {
				tokenEndpointURL
				credential {
				  ... on BasicCredentialData {
				  	username
					password
				  }
				  ...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				  }
			    }
				additionalHeaders
				additionalQueryParams
			}
			}
		}} }