mutation{result: updateRuntime(id: "211ddc82-01d7-4d2c-9b60-11c553776e35", in: {
		name: "runtime-1",
		description: "modified-runtime-1-description",
		labels: {
			ggg: ["hhh" ]
	}
		,
		annotations: {
			kkk: "lll"
	}
		
	}) {id
		name
		description
		tenant
		labels 
		annotations
		status {condition timestamp}
		agentAuth {credential {
				... on BasicCredentialData {
					username
					password
				}
				...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				}
			}
			additionalHeaders
			additionalQueryParams
			requestAuth { 
			  csrf {
				tokenEndpointURL
				credential {
				  ... on BasicCredentialData {
				  	username
					password
				  }
				  ...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				  }
			    }
				additionalHeaders
				additionalQueryParams
			}
			}
		}} }