mutation {
			result: addApplicationWebhook(applicationID: "38a548e0-fa7d-4b65-a6e3-a5bef7695727", in: {
		type: CONFIGURATION_CHANGED,
		url: "new-webhook",

	})  {id
		type
		url
		auth {
		  credential {
				... on BasicCredentialData {
					username
					password
				}
				...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				}
			}
			additionalHeaders
			additionalQueryParams
			requestAuth { 
			  csrf {
				tokenEndpointURL
				credential {
				  ... on BasicCredentialData {
				  	username
					password
				  }
				  ...  on OAuthCredentialData {
					clientId
					clientSecret
					url
					
				  }
			    }
				additionalHeaders
				additionalQueryParams
			}
			}
		
		}}
		}