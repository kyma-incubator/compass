apiVersion: v2
description: Kyma component 'ory'
name: ory
version: 1.1.0
home: https://kyma-project.io
icon: https://github.com/kyma-project/kyma/blob/main/logo.png?raw=true
dependencies:
  - name: gcloud-sqlproxy
    version: 0.25.1
    condition: global.ory.hydra.persistence.gcloud.enabled
  - name: oathkeeper
    version: 0.35.0
  # Hydra has no custom implementation, however, our Kubernetes servers are behind on new versions.
  # More specifically, the open source Hydra uses HPA with autoscaling/v2
  - name: hydra
    version: 0.35.0
