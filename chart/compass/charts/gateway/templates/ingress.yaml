apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ include "gateway.fullname" . }}
spec:
  rules:
    - http:
        paths:
          - path: /v1/gateway
            backend:
              serviceName: {{ include "gateway.fullname" . }}
              servicePort: 3000
{{/*          - path: /v1/director*/}}
{{/*            backend:*/}}
{{/*              serviceName: {{ include "registry.fullname" . }}*/}}
{{/*              servicePort: 3000*/}}
{{/*          - path: /v1/registry*/}}
{{/*            backend:*/}}
{{/*              serviceName: {{ include "registry.fullname" . }}*/}}
{{/*              servicePort: 3000*/}}
{{/*          - path: /v1/healthchecker*/}}
{{/*            backend:*/}}
{{/*              serviceName: {{ include "healthchecker.fullname" . }}*/}}
{{/*              servicePort: 3000*/}}