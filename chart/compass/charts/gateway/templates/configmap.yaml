apiVersion: v1
kind: ConfigMap
metadata:
{{/*  name: {{ template "gateway.fullname" . }}-auditlog-config*/}}
  name: {{ .Values.global.auditlog.configmap.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    release: {{ .Release.Name }}
data:
  auditlog-tenant: {{ .Values.global.auditlog.tenant}}
  auditlog-url: {{ .Values.global.auditlog.url }}
