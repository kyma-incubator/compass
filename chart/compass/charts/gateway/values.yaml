deployment:
  image:
    pullPolicy: IfNotPresent
  args:
    containerPort: &port 3000

service:
  port: *port

gateway:
  enabled: true

global:
  istio:
    gateway:
      name: "compass-istio-gateway"
      namespace: "compass-system"