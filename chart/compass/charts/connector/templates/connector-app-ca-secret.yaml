apiVersion: v1
data:
  # TODO: we should decide if those should be filled by Certs Support Job
  ca.crt: "{{ .Values.global.caCertificate }}"
  ca.key: "{{ .Values.global.caKey }}"
kind: Secret
metadata:
  name: {{ .Values.deployment.args.secrets.ca.name }}
  namespace: {{ .Values.deployment.args.secrets.ca.namespace }}