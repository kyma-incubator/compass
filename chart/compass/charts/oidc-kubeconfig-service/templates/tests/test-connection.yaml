apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oidc-kubeconfig-service.fullname" . }}-test-connection"
  labels:
{{ include "oidc-kubeconfig-service.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "oidc-kubeconfig-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
