deployment:
  image:
    pullPolicy: IfNotPresent

  securityContext: # Set on container level
    runAsUser: 2000
    allowPrivilegeEscalation: false

database:
  schemaFilePath: "/assets/database/provisioner.sql"

tests:
  gcp:
    credentials: ""
  hydraPublicUrl: http://ory-hydra-public.kyma-system:4444
  hydraAdminUrl: http://ory-hydra-admin.kyma-system:4445
  queryLogging: false
