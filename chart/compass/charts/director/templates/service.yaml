apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.directorPort }}
      targetPort: {{ .Values.service.directorPort }}
      name: grpc
  selector:
    app: {{ .Chart.Name }}
    release: {{ .Release.Name }}
