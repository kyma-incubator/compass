apiVersion: v1
kind: Service
metadata:
  name: {{ include "director.fullname" . }}
  labels:
    app: {{ include "director.name" . }}
    chart: {{ include "director.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.directorPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "director.name" . }}
    release: {{ .Release.Name }}
