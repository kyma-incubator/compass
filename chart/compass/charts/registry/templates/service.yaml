apiVersion: v1
kind: Service
metadata:
  name: {{ include "registry.fullname" . }}
  labels:
    app: {{ include "registry.name" . }}
    chart: {{ include "registry.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.registryPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "registry.name" . }}
    release: {{ .Release.Name }}
