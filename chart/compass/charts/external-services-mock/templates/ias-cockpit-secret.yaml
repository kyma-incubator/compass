{{- if eq .Values.global.externalServicesMock.ias.applyMockConfiguration true }}
apiVersion: v1
kind: Secret
metadata:
  name: ias-adapter-cockpit
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  ias-adapter-cockpit.yaml: |
    cert: |
      {{ .Values.global.connector.caCertificate | b64dec }}
    key: |
      {{ .Values.global.connector.caKey | b64dec }}
    ca: |
      {{ .Values.global.connector.caCertificate | b64dec }}
{{ end }}