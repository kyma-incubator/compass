apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
{{ include "kyma-env-broker.labels" . | indent 4 }}
  name: compass-kyma-environment-broker-pm
  namespace: compass-system
spec:
  endpoints:
   - port: http
     interval: "30s"
  namespaceSelector:
    matchNames:
    - compass-system
  selector:
    matchLabels:
{{ include "kyma-env-broker.labels" . | indent 8 }}
