apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.schemaVersionUpdateJob.cm.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    release: {{ .Release.Name }}
data:
  schemaVersion: " "