{{- if .Capabilities.APIVersions.Has "testing.kyma-project.io/v1alpha1" }}
apiVersion: "testing.kyma-project.io/v1alpha1"
kind: TestDefinition
metadata:
  name: {{ .Chart.Name }}-connection
  namespace: kyma-system
  labels:
    app: {{ .Chart.Name }}-tests
    app.kubernetes.io/name: {{ .Chart.Name }}-tests
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  disableConcurrency: false
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-tests
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: tests
        image: eu.gcr.io/kyma-project/external/curlimages/curl:7.70.0
        command: ["/usr/bin/curl"]
        args: [
          "--fail",
          "--max-time", "10",
          "--retry", "60",
          "--retry-delay", "3",
          "http://dex-service.kyma-system.svc.cluster.local:5556/.well-known/openid-configuration"
        ]
        securityContext:
{{- toYaml .Values.global.tests.securityContext | nindent 10 }}
      restartPolicy: Never
---
{{- end }}
