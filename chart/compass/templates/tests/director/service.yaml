apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-director-tests-ts
  namespace: {{ .Values.global.tests.namespace }}
  labels:
    app: {{ .Chart.Name }}-director-tests-app
spec:
  type: ClusterIP
  ports:
  - port: {{.Values.test.token.server.port}}
    targetPort: {{.Values.test.token.server.port}}
    protocol: TCP
    name: http-token-server
  selector:
    app: {{ .Chart.Name }}-director-tests-app