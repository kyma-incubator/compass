query {
    healthChecks(types:[MANAGEMENT_PLANE_APPLICATION_HEALTHCHECK],origin: "1") {
        data {
            type
            condition
            origin
            message
            timestamp
        }
    }
}