mutation {
    createEvent(applicationID: "123",in:  {
        spec: {
            data: ""
            eventSpecType: ASYNC_API,
        }
    }) {
        id
    }

    deleteEvent(id: 456) {
        id
    }


    updateEvent(id: 456, in:{
        spec: {
            eventSpecType: ASYNC_API,
            data: "",
        }
    }) {
        id
    }

    refetchEventSpec(id: 456) {
        id
        spec {
            fetchRequest {
                status {
                    condition
                    timestamp
                }
            }
        }
    }
}